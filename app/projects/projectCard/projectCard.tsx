import Image from "next/image";
import styles from "./avatar.module.css";

const projectCard = (prop: { title: string; desc:string; imgUrl: string }) => {
  return (
    <span></span>
  );
};

export default projectCard;
